# HQBase generates BETTER_AUTH_SECRET during installation.
# Add local-only values to `.dev.vars`; never commit secret values.
# BETTER_AUTH_SECRET=generate-a-local-secret
# HQBASE_LOCAL_SEED_PASSWORD=use-a-local-password-at-least-8-characters
